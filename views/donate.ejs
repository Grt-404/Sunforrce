<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Make a Donation</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/lucide@latest"></script>
    <style>
        body { font-family: 'Inter', sans-serif; }
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap');
    </style>
</head>
<body class="bg-gray-100 font-sans">

    <header class="bg-white shadow-sm">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4 flex justify-between items-center">
            <h1 class="text-xl font-bold text-indigo-600">Alumni Network</h1>
            <a href="/dashboard" class="flex items-center space-x-2 text-sm font-semibold py-2 px-4 rounded-lg transition-all duration-200 bg-gray-200 text-gray-700 hover:bg-gray-300">
                <i data-lucide="arrow-left" class="w-4 h-4"></i>
                <span>Back to Dashboard</span>
            </a>
        </div>
    </header>

    <main class="flex items-center justify-center min-h-[calc(100vh-150px)]">
        <div class="text-center p-8">
            <div class="bg-white p-10 rounded-2xl shadow-lg max-w-lg mx-auto">
                <div class="mx-auto mb-6 bg-green-100 rounded-full w-20 h-20 flex items-center justify-center">
                    <i data-lucide="gift" class="w-12 h-12 text-green-600"></i>
                </div>
                <h2 class="text-3xl font-extrabold text-gray-900">Support the Next Generation</h2>
                <p class="text-gray-600 mt-3 mb-8">Your contribution empowers students to achieve their dreams. Every donation, big or small, makes a significant impact on their educational journey.</p>
                
                <div class="mt-8">
                    <label for="donation-amount" class="sr-only">Donation Amount</label>
                    <div class="relative">
                        <div class="pointer-events-none absolute inset-y-0 left-0 flex items-center pl-4">
                            <span class="text-gray-500 text-2xl font-bold">$</span>
                        </div>
                        <input 
                            type="number" 
                            name="donation-amount" 
                            id="donation-amount"
                            class="w-full text-center text-3xl font-bold h-20 pl-12 pr-4 text-gray-800 border-2 border-gray-300 rounded-xl focus:ring-indigo-500 focus:border-indigo-500"
                            placeholder="50.00"
                        />
                    </div>
                </div>

                <button class="mt-8 w-full flex items-center justify-center space-x-3 text-lg font-bold py-4 px-6 rounded-xl transition-all duration-300 bg-indigo-600 text-white hover:bg-indigo-700 shadow-md hover:shadow-lg">
                    <i data-lucide="credit-card" class="w-6 h-6"></i>
                    <span>Proceed to Payment</span>
                </button>
            </div>
        </div>
    </main>

    <script>
        lucide.createIcons();
    </script>
</body>
</html>

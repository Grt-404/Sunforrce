<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Alumni Event Requests</title>
    <!-- Tailwind CSS from CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        @import url("https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap");
        body {
            font-family: "Inter", sans-serif;
        }
    </style>
</head>
<body class="bg-gray-100 min-h-screen p-4 sm:p-8">

    <div class="max-w-4xl mx-auto">
        <header class="flex items-center justify-between mb-8">
            <h1 class="text-4xl font-extrabold text-gray-800">Event Requests</h1>
            <!-- You can add a button or link here if needed, like "Back to Dashboard" -->
        </header>

        <main class="space-y-6">
            <% if (requests && requests.length) { %>
                <% requests.forEach(request => { %>
                <div class="bg-white p-6 rounded-2xl shadow-md border border-gray-200">
                    <h3 class="text-2xl font-bold text-gray-900 leading-tight mb-2"><%= request.title %></h3>
                    <p class="text-gray-600 mt-2"><%= request.description %></p>
                    
                    <form action="/alumni/eventrequests/accept/<%= request._id %>" method="POST" class="mt-4">
                        <button 
                            type="submit" 
                            class="w-full sm:w-auto bg-blue-600 text-white font-bold py-2 px-6 rounded-xl hover:bg-blue-700 transition-colors duration-300 transform hover:scale-105"
                        >
                            Accept
                        </button>
                    </form>
                </div>
                <% }) %>
            <% } else { %>
                <div class="bg-white p-6 rounded-2xl shadow-md text-center text-gray-500">
                    <p>No new event requests at this time.</p>
                </div>
            <% } %>
        </main>
    </div>
</body>
</html>

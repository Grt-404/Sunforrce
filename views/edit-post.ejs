<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Post</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 p-8">
  <div class="max-w-2xl mx-auto bg-white p-6 rounded-2xl shadow">
    <h2 class="text-xl font-bold mb-4">Edit Post</h2>
    <form action="/post/edit/<%= post._id %>" method="POST">
      <textarea name="content" rows="4"
        class="w-full p-3 border rounded-lg focus:outline-none"
        required><%= post.content %></textarea>
      <div class="mt-4 flex justify-between">
        <a href="/alumni/dashboard" class="text-gray-600 hover:underline">Cancel</a>
        <button type="submit" class="px-5 py-2 bg-blue-600 text-white rounded-lg">Save</button>
      </div>
    </form>
  </div>
</body>
</html>

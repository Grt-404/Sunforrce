<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Alumni Portal</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/lucide@latest"></script>
    <style>
        body { font-family: 'Inter', sans-serif; }
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap');
    </style>
</head>
<body class="bg-gray-100 font-sans">

    <!-- This EJS template assumes you pass a 'profile' object with user details -->

    <header class="bg-white shadow-sm">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4 flex justify-between items-center">
            <h1 class="text-xl font-bold text-indigo-600">Alumni Network</h1>
            <a href="/logout" class="flex items-center space-x-2 text-sm font-semibold py-2 px-4 rounded-lg transition-all duration-200 bg-gray-200 text-gray-700 hover:bg-gray-300">
                <i data-lucide="log-out" class="w-4 h-4"></i>
                <span>Sign Out</span>
            </a>
        </div>
    </header>

    <main class="flex items-center justify-center min-h-[calc(100vh-150px)]">
        <div class="text-center p-8">
            <div class="bg-white p-10 rounded-2xl shadow-lg max-w-md mx-auto">
                
                <p class="text-gray-500 mt-2 mb-8">Thank you for being a part of our community. Your support helps shape the future for current students.</p>
                
                <a href="/alumni/donate" class="w-full flex items-center justify-center space-x-2 text-lg font-bold py-4 px-6 rounded-xl transition-all duration-300 bg-green-500 text-white hover:bg-green-600 shadow-md hover:shadow-lg">
                    <i data-lucide="heart" class="w-6 h-6"></i>
                    <span>Donate Now</span>
                </a>
            </div>
        </div>
    </main>

    <script>
        lucide.createIcons();
    </script>
</body>
</html>

